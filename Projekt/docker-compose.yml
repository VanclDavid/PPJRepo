version: '3.8'
services:
  meteo:
      image: mcr.microsoft.com/devcontainers/java:0-17
      environment:
        - WATCHPACK_POLLING=true
      ports:
        - "8080:8080"
      volumes:
      - ./meteo:/meteo
      stdin_open: true
      tty: true